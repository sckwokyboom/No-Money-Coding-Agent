[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "no-money-coding-agent"
version = "0.0.1"
dependencies = [
  "requests>=2.31",
  "pyyaml>=6.0",
  "datasets>=2.20",
]